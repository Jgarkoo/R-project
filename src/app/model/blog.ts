export interface Blog {
    id: string
    author: string
    publish_date: string
    title: string
    image: string
    description: string
    categories: string[]
    email: string
  }
  
export interface categories {
    id: string
    title: String
    text_color: string
    background_color: string
}

  